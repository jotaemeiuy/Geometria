<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{Astro.props.title ?? "Aprende Geometría con p5.js"}</title>
    <meta
      name="description"
      content="Geometría básica con p5.js, lecciones paso a paso y ejemplos para estudiantes."
    />
    <meta
      name="keywords"
      content="geometría básica, p5.js, Processing, programación creativa, sketch, educación, JavaScript"
    />
    <meta name="author" content="Javier Martín" />
    <meta name="robots" content="index, follow" />
    <meta name="googlebot" content="index,follow" />
    <meta name="bingbot" content="index,follow" />
    <meta name="yandexbot" content="index,follow" />
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  @import "tailwindcss";
  @import "tailwind-animations";

  :root {
    color-scheme: dark;
    font-family:
      Inter,
      ui-sans-serif,
      system-ui,
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      sans-serif;
    background-color: #060914;
    color: #e2e8f0;
    text-rendering: optimizeLegibility;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  html,
  body {
    margin: 0;
    min-height: 100%;
    width: 100%;
    background:
      radial-gradient(circle at top, rgba(59, 130, 246, 0.14), transparent 28%),
      radial-gradient(
        circle at 80% 0%,
        rgba(56, 189, 248, 0.12),
        transparent 32%
      ),
      #050816;
  }

  body {
    overflow-x: hidden;
  }

  a {
    color: inherit;
    text-decoration: none;
  }

  ::selection {
    background: rgba(56, 189, 248, 0.25);
    color: #ffffff;
  }

  .prose {
    color: #cbd5e1;
    max-width: 72ch;
  }

  .prose h1,
  .prose h2,
  .prose h3,
  .prose h4 {
    color: #ffffff;
  }

  .prose p,
  .prose li,
  .prose blockquote,
  .prose dd {
    color: #cbd5e1;
    line-height: 1.9;
  }

  .prose a {
    color: #38bdf8;
    text-decoration: underline;
    text-underline-offset: 4px;
  }

  .prose a:hover {
    color: #7dd3fc;
  }

  .prose code {
    background: rgba(248, 250, 252, 0.08);
    border-radius: 0.45rem;
    padding: 0.25rem 0.4rem;
    color: #bae6fd;
    font-size: 0.95em;
  }

  .prose pre {
    background: rgba(15, 23, 42, 0.95);
    border: 1px solid rgba(148, 163, 184, 0.12);
    border-radius: 1rem;
    padding: 1rem 1.25rem;
    overflow-x: auto;
    box-shadow: 0 16px 48px rgba(15, 23, 42, 0.25);
  }

  .prose pre code {
    background: transparent;
    padding: 0;
    color: #e2e8f0;
    font-size: 0.95em;
  }

  .prose blockquote {
    border-left: 4px solid rgba(56, 189, 248, 0.55);
    padding-left: 1rem;
    color: #e2e8f0;
    background: rgba(15, 23, 42, 0.72);
    border-radius: 0.9rem;
  }

  .callout {
    display: block;
    border-radius: 1rem;
    border: 1px solid rgba(56, 189, 248, 0.18);
    background: rgba(15, 23, 42, 0.82);
    padding: 1.15rem 1.25rem;
    margin: 1.5rem 0;
  }

  .callout strong {
    display: block;
    color: #ffffff;
    margin-bottom: 0.4rem;
  }

  .lesson-card {
    border-radius: 1rem;
    border: 1px solid rgba(148, 163, 184, 0.16);
    background: rgba(15, 23, 42, 0.78);
    padding: 1.15rem 1.25rem;
    margin: 1.5rem 0;
  }
</style>
